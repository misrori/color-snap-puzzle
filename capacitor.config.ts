import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.misrori.colorsnappuzzle',
  appName: 'Tetra Puzzle',
  webDir: 'dist'
};

export default config;
